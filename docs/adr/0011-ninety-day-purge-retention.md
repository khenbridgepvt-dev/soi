# Ninety-day default purge retention

Soft-deleted records (cases, tasks) remain in the Archive for a configurable retention period. The default is **90 days** before they become eligible for permanent purge.

Only administrators can purge. Purge is irreversible. The retention period is configurable in system settings but defaults to 90 days given immigration cases span months and accidental deletions may not be noticed immediately.
