# Urgent case flag highlights active tasks only

When an administrator flags a case as urgent, only **active tasks** are highlighted red on the task board and related views — tasks with status `in_progress` or `not_started`. **Completed** tasks retain their normal appearance.

Case-level urgent highlighting (checklist header, dashboard badge) still applies to the whole case. Staff assigned to the case receive urgent notifications as before.
